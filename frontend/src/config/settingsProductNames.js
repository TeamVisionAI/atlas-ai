/** Product names for Settings — never translated. */
export const SETTINGS_TITLE = "Settings";

export const SETTINGS_SECTIONS = Object.freeze({
  profile: "Profile",
  organization: "Organization",
  whatsapp: "WhatsApp",
  scheduling: "Scheduling",
  appointments: "Appointments"
});

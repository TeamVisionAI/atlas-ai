# Committed public staging frontend config for Vercel Preview (`VERCEL_ENV=preview`).
# No secrets. Do not put bootstrap tokens, JWT, service_role, or DB passwords here.
# Vercel Production must keep using `--mode production` and must not load this file.

VITE_ATLAS_ENV=staging
VITE_API_BASE_URL=https://atlas-ai-staging-staging.up.railway.app
VITE_META_REVIEW_MODE=false

VITE_META_EMBEDDED_SIGNUP_CONFIG_ID=1047589024793080
